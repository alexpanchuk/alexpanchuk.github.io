=== 1. CSS
изменить способ вращения transorm-origin
установить все стрелки на 12
transition 0.5s
transition-timing-function можно подобрать в DevTools
=== 2. мой вариант
прослушиваем Timer (или что-то подобное)
считываем часы, минуты, секунды
поворачиваем стрелку на 90 + (минуты/секунды / 60 * 360)
                        90 + (часы / 12 * 360)
=== 3. как в видео
создаем объект типа Date()
считываем часы, минуты, секунды
считаем углы, прибавляем 90
стилизуем DOM element
запускаем функции через setInterval каждые 1000ms
при переходе на 0 нужно удалить transition
==========
setInterval(foo, 1000ms)
now = new Date(); - создаем объект типа Date
 методы .getSeconds()
	.getMinutes()
	.getHours()

obj.styles.transform = 